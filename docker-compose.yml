version: "3.9"

services:
  sentinel-ai:
    build: .
    container_name: sentinel-ai-server
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./token.json:/app/token.json
      - ./credentials.json:/app/credentials.json
